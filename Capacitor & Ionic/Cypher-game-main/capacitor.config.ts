import type { CapacitorConfig } from '@capacitor/cli'

const config: CapacitorConfig = {
  appId: 'com.cypherGame.app', // using reverse domain format 
  appName: 'Cypher Game',
  webDir: 'dist',
}

export default config